# AI Engineer 안주연

**AI Agent Workflow · RAG/LLM · Frontend Service**

> (한 줄 포지셔닝 문장 — 예: "데이터의 신뢰성을 의심하고, 에이전트로 검증해 진짜 인사이트를 만드는 AI Engineer" — 직접 다듬어주세요)

---

# VOD 시청 로그 기반 이탈·매출 진단 멀티에이전트 AI 시스템

**LG HelloVision 시청 데이터 기반 이탈 · 구매 예측 모델링**

> "성능 개선은 모델 교체보다 데이터 구조·라벨 정의 재설계에서 시작된다는 점을 확인했습니다."
> "이 경험을 통해 모델보다 먼저 데이터셋, 라벨, 피처를 같이 설계하는 방식으로 예측 문제를 바라보게 됐습니다."

---

## 01. 프로젝트 개요

LG헬로비전 시청·해지·VOD 구매 로그를 기반으로, **"매출 = 고객 수 × 고객당 매출"** 구조의 두 축을 ML로 진단하는 시스템.

| 항목 | 내용 |
|------|------|
| **Period** | 1차(팀) 2025.MM~2025.MM · 2차(개인) 2026.MM~2026.MM *(정확한 월 입력 필요)* |
| **Team** | **1차(EDA·모델링)**: 4인 팀 프로젝트 · **2차(에이전트 적용·재검증)**: 개인 후속 프로젝트 |
| **Role** | 팀 내 역할(1차): 데이터 검증 · 모델 평가 / 개인 기여(2차): ML Agent Pipeline 설계 · 타깃누수 자동검증 |
| **Revenue Context** | LG헬로비전 2025년 연간 매출 1조 2,657억원(전년대비 +5.8%) 중 상품(렌탈·교육단말) 26.0% 확대 · 방송(헬로TV) 22.2% 감소(-2.8%) → 방송 고객 유지 + VOD 구매 행동 분석 필요 |
| **Business Frame** | 매출 = 고객 수 × 고객당 매출 |
| **Task** | 고객 수 유지 → **해지 예측** (분류, PRC-AUC) · 고객당 매출 증대 → **VOD 구매 예측** (회귀, R²) — ※ RVOD 구매건수 = 매출 proxy (원본 데이터에 실제 매출·요금 필드 없음) |

### 배경 · 문제 · 해결

| 단계 | 내용 |
|------|------|
| 배경 | TV 시청은 줄고 있는데, 이게 진짜 이탈 신호인지 불분명 |
| 문제 | ① 어떤 변수가 진짜 신호고 어떤 게 타깃누수인지 모름 ② 적절한 Y(종속변수)가 무엇인지도 불분명 |
| 해결 | `leakage_checker` 에이전트로 변수별 누수 자동탐지 + 후보 Y들의 실제 발생률을 정량 검증해 타깃 확정 |

### 데이터 수집

- Raw CSV 3종(user_profile 45.3M행 · vod_log 60.7M행 · vod_content 47만행) → DuckDB 적재 → Parquet 변환
- 저장 용량 **66.5%↓**, DuckDB 집계 기준 **1,333배** 속도 향상
- 사용자(`sha2_hash`) × 월(`p_mt`) 패널 구조, 2023.01~2023.12

### 사용자 시나리오

이탈 가능 고객과 VOD 구매 가능 고객을 예측해 고객 유지·구매 유도 전략 수립에 활용

---

## 02. 적용기술 — 모델링 / Agent

### 아키텍처

```
Raw CSV → DuckDB 적재 → Parquet 변환(EDA·클린징) → churn/VOD 데이터셋 빌드 → 모델링 → 평가(Eval Agent) → 서빙
```

### Agent 흐름

```
Supervisor → Data Profiler → Leakage Checker → EDA Agent → Feature Engineering → Modeling Agent → Explanation Agent
                                                                  ↓ (게이트 미달 시)
                                                          Human-in-the-Loop
```

LangGraph로 오케스트레이션, LangSmith로 실행 추적.

### 핵심 기술

| 영역 | 내용 |
|------|------|
| 모델 | Logistic Regression / Random Forest / Gradient Boosting / LightGBM / XGBoost 5종 비교 |
| 해지 예측 | 이진분류, 불균형 80:20, PRC-AUC 기준 (불균형 데이터라 ROC-AUC 대신 채택) |
| VOD 구매 예측 | 회귀, zero-inflated(구매 1건↑ 비율 3.1%), R²/MAE 기준 |
| 재현성 | SEED=42 고정, `sha2_hash` 사용자 단위 샘플링 (행 단위 분할 시 발생하는 사용자 누수 방지) |
| 인프라 | DuckDB `:memory:` 대용량 집계, `BASE=Path(__file__).resolve().parent.parent.parent` 경로 패턴 |

---

## 03. AI Native — 만드는 방식

### 설계 방식 (2단계로 진행됨)

이 프로젝트는 **처음부터 에이전트로 설계되지 않았습니다.**

1. **1차 (팀 프로젝트)** — 스크립트 기반으로 EDA → 변수선정 → 모델링을 순차 진행, XGBoost PRC-AUC 0.521(PASS 판정)로 완료
2. **2차 (개인 후속 프로젝트)** — 이후 AI 에이전트 워크플로 개념을 개인적으로 학습 → "완료로 판단한 프로젝트도 자동 검증 에이전트를 거치면 다른 결과가 나올까?"를 직접 검증하기 위해 `leakage_checker` 등 검증 에이전트를 혼자 설계·구현해 기존 프로젝트에 후속 적용 — 11-agent 파이프라인, LangGraph 오케스트레이션, Spec Kit 기반 워크플로 설계까지 전 과정 개인 작업

### 검증 방식

- `leakage_checker` 에이전트가 컬럼명 패턴(END/CANCEL/STOP 토큰)과 importance 분석으로 타깃누수 자동탐지
- 결과: `VOC_STOP_CANCEL_MONTH1_YN` · `AGMT_END_YMD` · `AGMT_END_SEG` 3개 변수가 critical 누수로 판정 → 제거 후 재학습하면 PRC-AUC 0.521 → **0.475로 게이트 미달**
- **왜 1차에서 못 잡았나**: 이 3개 변수의 실제 타깃 상관계수는 0.10 · -0.03 · 0.04로, 상관계수만 봤으면 평범한 약한 변수로 보여 전혀 위험해 보이지 않았음. 1차 팀의 역량 문제가 아니라 — 컬럼명·시점 의미(언제 기록되는 값인가)까지 봐야 잡히는, 통계만으로는 안 보이는 종류의 문제였음. 그래서 상관계수·importance·컬럼명 패턴·중복키를 함께 보는 체계적 검증 단계가 필요함을 직접 증명
- `eval-agent`가 PRC-AUC ≥ 0.5 / R² ≥ 0.6 / MAE ≤ 0.6 기준을 자동 검증, 미달 시 Human-in-the-Loop 트리거

### 협업 · 자동화

- Claude Code + `CLAUDE.md`로 프로젝트 컨텍스트·코드 규칙을 표준화해 에이전트 간 일관성 확보
- HITL 개입 트리거 4가지: 지표 미달 / 데이터 드리프트 / 누수 의심 / 배포 결정

---

## 04. 결과 — 성능지표 / 화면

### 1차 성과 (팀 프로젝트 — EDA·모델링)

| 태스크 | 지표 | 1차 | +파생변수 | 개선폭 |
|---|---|---|---|---|
| 해지 예측 | PRC-AUC | 0.478 (LightGBM) | **0.521** (XGBoost) | +0.043 |
| VOD 구매 예측 | R² | 0.293 | **0.624** (Random Forest, MAE 0.596) | +0.331 |

핵심 변수: `lagged_rvod_cnt`(전월 구매건수) — **"돈을 써본 사람이 또 쓴다"**

### 2차 성과 (개인 — 에이전트 적용·재검증)

2차의 성과는 **수치를 더 올린 게 아니라, 1차 수치를 믿어도 되는지 검증해서 "정직한 베이스라인"을 확보한 것**입니다.

| 태스크 | 1차 보고 수치 | 누수 제거 후(재검증) | 결과 |
|---|---|---|---|
| 해지 예측 | PRC-AUC 0.521 | **0.475** | 게이트(0.5) 미달 |
| VOD 구매 예측 | R² 0.624 | **0.525** | 게이트(0.6) 미달 |

- `leakage_checker` 에이전트로 `VOC_STOP_CANCEL_MONTH1_YN`·`AGMT_END_YMD`·`AGMT_END_SEG` 3개 컬럼이 **두 모델 모두에** critical 누수로 들어가 있었음을 발견
- VOD 모델은 추가로 `vod_view_cnt`·`vod_use_tms_sum`·`vod_asset_cnt`(월별이 아닌 **전체기간 누적** — 미래 데이터가 새는 구조)까지 발견, ablation 실험으로 직접 정량 검증
- 정적 룰 기반 탐지(`leakage_checker`: 컬럼명 패턴 + 상관계수)의 한계도 발견 — 시간윈도우 누수는 이 규칙으로 못 잡혀서, **학습 후 ablation으로 보완하는 2단계 검증체계**를 직접 설계

### TV → VOD 전환 분석 (1차 인사이트)

| 그룹 | 해지율 |
|------|--------|
| 저TV·고VOD (TV 하위25% + VOD 상위25%) | **15.93%** |
| 그 외 대조군 | 8.79% |

→ **TV 시청 감소 ≠ 이탈.** 소비 방식이 TV → VOD로 확장되는 현상.

### 화면

- `output/tv_vod_shift/monthly_tv_vod_trend.png` — TV-VOD 월별 트렌드
- `output/churn_analysis/risk_segment_churn.png` — 위험 세그먼트별 해지율
- `output/churn_analysis/radar_content_churn.png` — 콘텐츠 유형별 해지 레이더
- `output/churn_analysis/flag_kids_movie_churn.png` — 키즈/영화 시청 플래그별 해지율
- `output/churn_analysis/bubble_content_churn.png` — 콘텐츠 시청량·해지율 버블차트

---

## 05. Insight — 배운 역량 / 회사에서 할 수 있는 일

### 이 프로젝트를 통해 배운 역량

- 성능 개선은 모델 교체보다 **데이터 구조·라벨 정의 재설계**에서 시작된다
- Y(종속변수) 선택은 비즈니스적 의미보다 먼저 **데이터의 실제 발생률**을 정량적으로 검증해야 한다 — 대안 Y 후보(상품등급변경·결합상품가입 등)가 월간 0.03~0.3%로 너무 희귀해 기각된 사례로 확인
- "끝났다"고 판단한 프로젝트도 에이전트 기반 자동 검증으로 재검토하면 숨어있던 결함(타깃누수)을 발견할 수 있다
- "매출"처럼 강한 단어를 쓰기 전에 데이터에 그 필드가 실제로 존재하는지부터 확인해야 한다 — RVOD 건수는 매출이 아니라 매출 proxy

### 내가 회사에서 할 수 있는 일

- 기존 ML 파이프라인에 **자동 검증 에이전트**(leakage check, 성능 게이트)를 추가해 타깃누수·데이터품질 이슈를 출시 전에 차단
- 단발성 모델 학습이 아니라, **측정 가능한 게이트(PRC-AUC/R²)를 가진 반복 가능한 실험 파이프라인** 구축
- "이미 완료된" 레거시 분석·모델도 의심하고 재검증하는 워크플로를 도입해 의사결정 리스크를 줄임
