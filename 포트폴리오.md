# LG HelloVision 시청 데이터 기반 이탈 · 구매 예측 모델링

> "성능 개선은 모델 교체보다 데이터 구조·라벨 정의 재설계에서 시작된다는 점을 확인했습니다."
> "이 경험을 통해 모델보다 먼저 데이터셋, 라벨, 피처를 같이 설계하는 방식으로 예측 문제를 바라보게 됐습니다."

---

## Page 1 — 배경 & 데이터 설계 (Why + How)

### 비즈니스 배경

LG헬로비전 2025년 1분기 매출 구조:

| 부문 | 비중 |
|------|------|
| 상품 (렌탈) | 28% |
| 방송 (헬로TV) | 23% |
| 부가서비스 | 21% |

VOD는 방송·부가서비스의 **핵심 수익 채널**이다.

### 프로젝트 목적

**매출 = 고객 수 × 고객당 매출**

이 공식에서 두 가지 과제가 도출된다:

- **① 고객 수 유지** → 해지 예측 (Churn Prediction)
- **② 고객당 매출 증대** → VOD 구매 예측 (RVOD Purchase Prediction)

---

### 데이터 파이프라인

```
Raw CSV (3종)  →  DuckDB 적재  →  Parquet 변환  →  품질 검증  →  fin_data  →  분석
log / mart / tps    조회·가공       변환+타입최적화    결측/조인 점검   통합 데이터셋   Churn / RVOD
```

- 최종 데이터셋: **44.9M rows / 60 cols / 6.76GB**
- 저장 용량 **▼73.1%** / 로딩 시간 **▼37.6%**
- 포맷 전환: CSV → Parquet (컬럼형 저장, DuckDB 기준 **1,333배** 속도)

---

### 모델링을 위한 5가지 데이터 설계 결정

#### ① 결측 컬럼 제거

**Why**
결측률 70% 이상 컬럼은 정보량보다 노이즈가 커 학습을 방해한다.

**How**
- vod_content 88컬럼 → **55컬럼** (결측 70%↑ 제거)
- 고유값 1개 이하 컬럼도 추가 제거 (분산이 없어 예측력 없음)

---

#### ② 사용자 단위 샘플링

**Why**
행 단위로 샘플링하면 같은 사용자(sha2_hash)의 데이터가 train/test에 나뉘어
사용자 행동 패턴이 학습 단계에 이미 노출되는 **데이터 누수**가 발생한다.

**How**
- sha2_hash 기준으로 **사용자 단위** 샘플링
- SEED=42 고정으로 재현성 확보

---

#### ③ 조인 매칭률 검증

**Why**
미매칭 행을 무조건 삭제하면 특정 사용자군이 편향적으로 제거될 수 있다.

**How**
- sha2_hash 매칭률: **71.0%** 확인
- asset 매칭률: **93.2%** 확인
- 미매칭 행은 삭제하지 않고 **단독 피처로 유지**

---

#### ④ Churn 라벨 재정의

**Why**
기존 cancel_yn은 현재 시점의 상태값이다.
이 경우 이미 이탈한 시점 이후의 행동 데이터가 학습에 포함되어
**라벨 시점 오류(Label Leakage)**가 발생한다.

**How**
- 실제 이탈이 발생한 달을 기준으로 **사용자×월 단위로 라벨 재정의**
- 현재 상태값이 아닌 미래 이탈 시점을 예측하도록 설계

---

#### ⑤ 누수 변수 차단

**Why**
이탈과 동시에 발생하거나 이탈 이후에만 나타나는 변수가 피처에 포함되면
모델이 미래 정보를 학습하는 **피처 누수(Feature Leakage)**가 발생한다.

**How**
- 사용자×월 기준으로 집계된 피처만 사용
- VOC 불만 접수 등 이탈과 동시 발생하는 변수 사전 차단
- 레이블 포함 변수 사전 식별 및 제거

---

## Page 2 — 모델링 결과 (What)

### Churn 예측 — 반복 개선 (v1 → v2)

| | v1 | v2 |
|---|---|---|
| 최고 모델 | LightGBM | **XGBoost** |
| Val PRC-AUC | 0.4782 | **0.5214** |
| 개선폭 | — | **+0.043** |
| 변수 수 | 10개 | 17개 (+7개) |

#### v2 신규 추가 변수 (7개)

| 변수 | 의미 |
|------|------|
| Recency | 마지막 VOD 시청 이후 경과일 — 비활성 고객 탐지 |
| Log_Watch_Time | 총 시청 시간 로그 변환 — 비선형 패턴 포착 |
| Flag_Kids | 키즈 콘텐츠 시청 패턴 플래그 |
| Flag_Movie | 영화 콘텐츠 시청 패턴 플래그 |
| Total_Watch_Time | 누적 VOD 시청 시간 |
| Is_Netflix | 넷플릭스 이용 여부 |
| Risk_Segment_Kids_NFX | 키즈+넷플릭스 고위험군 세그먼트 |

> CT_CL 분포 분석을 통해 키즈·영화 시청 패턴이 해지와 연관됨을 발견, 파생변수로 추가

#### 상위 5개 변수 가설검정 (p-value = 0.0, 전부 귀무가설 기각)

| 변수 | 검정 | 효과크기 |
|------|------|---------|
| AGMT_END_YMD | t-test | Cohen's d = 0.063 |
| TOTAL_USED_DAYS | t-test | Cohen's d = 0.247 |
| CH_HH_AVG_MONTH1 | t-test | Cohen's d = 0.258 |
| AGMT_END_SEG | chi² | Cramer's V = 0.157 |
| CH_LAST_DAYS_BF_GRP | chi² | Cramer's V = 0.142 |

**첨부 이미지**
- `output/churn_analysis/model_comparison.png` — v1 모델별 성능 비교
- `output/churn_analysis/churn_v2_model_comparison.png` — v2 모델별 성능 비교

---

### VOD 구매 예측 — Random Forest

| 지표 | 값 |
|------|---|
| R² | **0.624** |
| MAE | **0.596** |
| RMSE | 7.74 |

**핵심 변수:** `lagged_rvod_cnt` (전월 유료 구매 건수)

> **"돈을 써본 사람이 또 쓴다"** — 과거 구매 경험이 미래 구매를 예측하는 가장 강한 변수

**모델 활용 시나리오**
- 비구매자 대상: 첫 구매 유도 프로모션
- 구매 경험자 대상: 개인화 추천·업셀링 타겟팅

**첨부 이미지**
- `output/vod_purchase_model/model_comparison.png` — 모델별 R²/MAE/RMSE 비교

---

### TV → VOD 전환 분석

| 그룹 | 해지율 |
|------|--------|
| 저TV·고VOD (TV 하위 25% + VOD 상위 25%) | **15.93%** |
| 그 외 대조군 | 8.79% |
| VOD 최다 이용 (Q5) | 12.26% |
| VOD 최소 이용 (Q1) | 10.22% |

**핵심 인사이트**
TV 시청 감소 ≠ 이탈.
소비 방식이 TV에서 VOD로 **확장**되는 중이다.
→ VOD 중심 수익 구조 재설계 및 유료 VOD 활성화로 ARPU 방어 필요

**첨부 이미지**
- `output/tv_vod_shift/monthly_tv_vod_trend.png` — 월별 TV·VOD 시청 트렌드

---

### 채널 시청 예측 — LightGBM

| 모델 | R² | MAE | RMSE |
|------|-----|-----|------|
| Linear Regression | 0.043 | 3.19 | 4.00 |
| Random Forest | 0.866 | 1.02 | 1.50 |
| Gradient Boosting | 0.866 | 1.02 | 1.50 |
| **LightGBM** | **0.866** | **1.02** | **1.50** |

- 종속변수: `CH_HH_AVG_MONTH1` (월평균 채널 시청량)
- 독립변수: log_VOD_TIME · Adult · FOD · Trend · INHOME_RATE · AGE_GRP10
- Linear R² 0.043 vs Tree R² 0.866 → **강한 비선형 관계** 확인

**첨부 이미지**
- `output/channel_model/model_comparison.png` — 모델별 성능 비교

---

### 모델 통합 활용 시나리오

| 고객 유형 | 전략 |
|-----------|------|
| 이탈 위험 + 미구매 | 긴급 유지 + 첫 구매 패키지 |
| 이탈 위험 + 구매 경험 | 유지 프로모션 + 재구매 타겟팅 |
| 이탈 안전 + 미구매 | 첫 구매 유도 프로모션 |
| 이탈 안전 + 구매 경험 | 개인화 추천·업셀링 확대 |

---

## Page 3 — AI-Native Engineering

### 설계지침서 6단계

| 단계 | 내용 | 산출물 |
|------|------|--------|
| **1단계** | 실험 환경 표준화 | ml-env.md · copilot-instructions.md |
| **2단계** | 데이터/모델 프롬프트 설계 | eda-report.prompt.md · model-arch.prompt.md |
| **3단계** | ML 전문 에이전트 구성 | data-scientist · ml-ops · eval · control agent |
| **4단계** | 실험 컨텍스트 설계 | EXPERIMENT.md — 하이퍼파라미터·샘플링 기록 |
| **5단계** | 실험 자동화 사이클 | TDD-ml-cycle.md — Red/Green/Refactor |
| **6단계** | 통합 검증 및 서빙 | INFERENCE_REPORT.md — 추론·검증 리포트 |

---

### 에이전트 아키텍처

```
Control Agent  (흐름·라우팅·오케스트레이션)
├── Data Scientist Agent  →  피처 엔지니어링 · 변수 선정 · 가설검정
├── ML Ops Agent          →  서빙 환경 최적화 · Parquet · DuckDB 파이프라인
├── Eval Agent            →  PRC-AUC ≥ 0.521 / R² ≥ 0.624 / MAE ≤ 0.60 자동 검증
└── Human-in-the-Loop     →  지표 미달·배포 결정 시 사람 개입 트리거
```

**Human-in-the-Loop 개입 트리거**
- PRC-AUC < 0.50 또는 R² < 0.40 → 자동 중단 후 review_request.md 생성
- Feature Importance 상위 순위 ±3 이상 급변 → 데이터 드리프트 의심
- 배포 결정 → 사람 최종 승인 필수 (Eval Agent 단독 불가)
- 라벨 재정의 또는 피처 누수 의심 → 즉시 에스컬레이션

---

### TDD ML 사이클

| 단계 | 의미 | 행동 |
|------|------|------|
| 🔴 Red | 지표 미달 확인 | 실패 테스트 작성 → 원인 분석 시작 |
| 🟢 Green | 목표 달성 | 최소 변경으로 기준 통과 (변수 추가·하이퍼파라미터 조정) |
| 🔵 Refactor | 재현성 확보 | 코드·파이프라인 정리 / SEED=42 / 경로 패턴 표준화 |

---

### 데이터 파이프라인 설계 원칙

| 원칙 | 내용 |
|------|------|
| 경로 패턴 | `BASE = Path(__file__).resolve().parent.parent.parent` — 하드코딩 금지 |
| 샘플링 재현성 | SEED=42 고정 / Reservoir 샘플링 / sha2_hash 사용자 단위 |
| DuckDB 활용 | `:memory:` 연결로 대용량 집계 / Parquet → 66.5% 용량 절감 |
| 출력 구조 | `output/{태스크명}/` 하위 CSV·PNG 저장 |

---

### Harness & Claude Code 실행 환경

**Harness 구성 요소**
- Runner — 에이전트 실행 컨테이너
- Context Injector — 프롬프트에 데이터 컨텍스트 주입
- Prompt Manager — 프롬프트 버전 관리·롤백
- Output Validator — Eval Agent 연동 자동 검증

**Claude Code 활용**
- `/init` → CLAUDE.md 자동 생성 (프로젝트 지침 문서화)
- Agent SDK → 멀티 에이전트 오케스트레이션
- MCP 서버 연동 (Figma · DuckDB)
- 실행 환경: VS Code + GitHub Copilot / Python 3.13 / DuckDB `:memory:`

---

## 첨부 이미지 목록

| 페이지 | 설명 | 파일 경로 |
|--------|------|-----------|
| Page 2 | Churn v1 모델 비교 | `output/churn_analysis/model_comparison.png` |
| Page 2 | Churn v2 모델 비교 | `output/churn_analysis/churn_v2_model_comparison.png` |
| Page 2 | VOD 구매 모델 비교 | `output/vod_purchase_model/model_comparison.png` |
| Page 2 | TV-VOD 월별 트렌드 | `output/tv_vod_shift/monthly_tv_vod_trend.png` |
| Page 2 | 채널 시청 모델 비교 | `output/channel_model/model_comparison.png` |
