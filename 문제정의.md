# VOD 시청 로그 기반 이탈·매출 진단 멀티에이전트 AI 시스템

## 문제정의

## 1. 비즈니스 배경

LG헬로비전 2025년 연간 사업보고서 기준(2026.02 발표) 매출 1조 2,657억원(전년대비 +5.8%) 중 상품(렌탈·교육단말) 26.0%, 방송(헬로TV) 22.2%(전년대비 -2.8%)를 차지한다. 방송 비중이 줄고 상품·렌탈 비중이 늘어나는 추세이며, VOD는 방송 매출에 포함되는 핵심 채널이다(상품·통신/MVNO과는 무관).

## 2. 매출 구조와 문제 분해

```
매출 = 고객 수 × 고객당 매출
```

이 공식을 두 개의 독립적인 ML 문제로 분해한다.

| 축 | 비즈니스 과제 | ML 문제 |
|----|--------------|---------|
| 고객 수 | 고객 수 유지 | 해지 예측 (분류) |
| 고객당 매출 | 고객당 매출 증대 | VOD 구매 예측 (회귀) |

## 3. 문제 1 — 해지 예측

| 항목 | 정의 |
|------|------|
| 종속변수(Y) | `cancel_yn` (유지=0, 해지=1) |
| 단위 | 사용자 × 월 (`sha2_hash`, `p_mt`) |
| 태스크 | 이진 분류 |
| 데이터 | `churn_dataset`(1차) → `churn_dataset_v2`(+7변수) → `churn_dataset_v3`(+갤럽 트렌드 3변수) |
| 클래스 비율 | 유지:해지 = 80:20 (해시 기반 샘플링, SEED=42) |
| 평가지표 | PRC-AUC ≥ 0.5 (불균형 데이터 특성상 ROC-AUC 대신 PRC-AUC 채택) |

**1차(팀 프로젝트) 결과**: 문서(`docs/TDD-ml-cycle.md`)에는 XGBoost PRC-AUC 0.521(PASS)로 기록.

**2차(개인 후속) 재검증**: `scripts/agents/leakage_checker.py`(2026-06-21) 검증 결과 `VOC_STOP_CANCEL_MONTH1_YN`·`AGMT_END_YMD`·`AGMT_END_SEG` 3개 변수가 **타깃누수(critical)**로 판정됨. 이 3개를 제외하고 동일 조건으로 재학습하면 XGBoost PRC-AUC가 **0.475로 게이트 미달**. 즉 1차의 0.521 기록은 누수가 섞인 수치였고, 정직한 베이스라인은 0.475(미달)다. 2차의 성과는 수치를 올린 게 아니라 **부풀려진 1차 수치를 검증해 정직한 베이스라인을 확보한 것**이다.

## 4. 문제 2 — VOD 구매 예측

| 항목 | 정의 |
|------|------|
| 종속변수(Y) | `vod_purchase_cnt` (= RVOD 월별 시청 건수, 유료 VOD 구매/렌탈) |
| 단위 | 사용자 × 월 |
| 태스크 | 회귀 (zero-inflated, 구매 1건 이상 비율 약 3.1%) |
| 데이터 | `vod_purchase_dataset` (churn_dataset 기반 + FOD/RVOD/SVOD 건수 + 전월 RVOD lag) |
| 평가지표 | R² ≥ 0.6, MAE ≤ 0.6 |

**1차(팀 프로젝트) 결과**: Random Forest R² 0.624, MAE 0.596 (PASS 기록).

**2차(개인 후속) 재검증**: leakage_checker가 `vod_purchase_dataset`에도 해지 모델과 동일한 3개 컬럼(`VOC_STOP_CANCEL_MONTH1_YN`·`AGMT_END_YMD`·`AGMT_END_SEG`)을 critical로 플래그했고, 추가로 `vod_view_cnt`·`vod_use_tms_sum`·`vod_asset_cnt`가 **월별이 아니라 전체기간 누적치**(미래 데이터가 새는 구조)임을 직접 발견. 이 6개 피처를 모두 제외하고 동일 조건(RandomForest, Optuna 튜닝)으로 재학습하면 R²가 **0.593 → 0.525로 하락, 게이트(0.6) 미달**. 정적 룰 기반 `leakage_checker`는 컬럼명 패턴·상관계수로만 탐지해서 이 시간윈도우 누수는 못 잡았고, 학습 후 ablation 실험으로 사람이 직접 찾아냈다 — 즉 사전 자동탐지만으로는 불충분하며 사후 검증이 함께 필요하다.

**중요한 전제 — "매출"이 아니라 "매출 proxy"**: `user_profile`(39열)·`vod_content`(88열)·`vod_log`(9열) 스키마를 전수 확인한 결과, 원화(₩) 단위의 매출·요금·결제금액·ARPU 필드가 원본 데이터에 전혀 없다. `vod_content.prpgt_pri`도 값이 0~10인 가격 등급 코드일 뿐 금액이 아니다. 따라서 `vod_purchase_cnt`는 매출 그 자체가 아니라 **"구매 행동(건수) 기반 매출 proxy"**로 한정해서 해석·보고해야 한다.

## 5. 검토했으나 채택하지 않은 대안 Y

매출 구조에 더 직접적으로 연결되는 이벤트들(상품등급 변경, 결합상품 가입/해지, 약정축소)을 패널 데이터(`user_profile_clean.parquet`, 202305→202306 1개월 전이)로 직접 검증했다.

| 후보 | 의미 | 월간 발생률 |
|------|------|------------|
| 상품등급 상향 | 업셀 | 0.029% |
| 상품등급 하향 | 다운셀 | 0.27% |
| 결합상품 신규가입 | 크로스셀 | 0.25% |
| 결합상품 해지 | 매출 감소 | 0.05% |
| 약정축소 | 계약 축소 | 0.12% |

기존 두 타깃(해지 20%, VOD구매 3.1%)보다 10~100배 희귀해 11개월 패널로는 ML 타깃으로 표본이 부족하다. 등급·결합상품은 가입 시점에 정해지면 거의 변하지 않는 정적 속성이라 전이 자체가 드물다 — **현재 두 타깃(`cancel_yn`, `vod_purchase_cnt`) 조합이 이 데이터 안에서 현실적으로 최선의 선택**이다.

## 6. 핵심 가설 (분석으로 검증됨)

**TV 시청 감소 ≠ 이탈.** 저TV·고VOD 그룹의 해지율(15.93%)이 대조군(8.79%)보다 높지만, 이는 "TV 이탈" 신호가 아니라 소비 채널이 TV → VOD로 **확장**되는 현상이다. → 두 문제(해지/VOD구매)는 독립된 두 축이 아니라, 같은 수익구조를 바라보는 상호작용하는 두 측면으로 다뤄야 한다.

## 7. 다음 액션

- ~~VOD 구매 모델: `vod_purchase_dataset`에서도 동일 누수 3컬럼 제외 후 R²/MAE 재검증~~ → **완료** (0.624→0.525, 게이트 미달 확인)
- 해지 모델: `churn_dataset_v3`(갤럽 트렌드 변수) 기반으로 누수 3컬럼 제외 후 게이트(PRC-AUC ≥ 0.5) 재통과 여부 검증
- 보고서·발표 자료에서 "매출 예측"이라는 표현 대신 "매출 proxy(구매행동) 예측"으로 일관되게 표기
- 1차(팀)/2차(개인) 성과는 "수치 개선"이 아니라 "정직한 베이스라인 확보"로 일관되게 서술
